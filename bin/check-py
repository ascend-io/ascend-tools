#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."

echo "Running ruff..."
uv run ruff check .
uv run ruff format --check .

echo "Running ty..."
uv run ty check

echo "Python checks passed!"
