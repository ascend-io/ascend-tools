#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."

echo "Installing ascend-tools-cli..."
cargo install --path src/ascend_tools/ascend-tools-cli

echo "Rust install complete!"
