#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/../src/ascend_tools"

echo "Installing ascend-tools-cli..."
cargo install --path ascend-tools-cli

echo "Rust install complete!"
