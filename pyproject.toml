[project]
name = "ascend-ops"
version = "0.1.0"
description = "SDK and CLI for the Ascend REST API"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "Cody", email = "cody.peterson@ascend.io" }]
dependencies = []

[project.scripts]
ascend-ops = "ascend_ops.cli:main"

[dependency-groups]
dev = ["maturin>=1.0,<2.0", "ruff>=0.9", "pytest>=8"]

[tool.maturin]
module-name = "ascend_ops.core"
python-packages = ["ascend_ops"]
python-source = "src"
manifest-path = "src/ascend_ops/ascend-ops-py/Cargo.toml"

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.uv]
cache-keys = [
  { file = "pyproject.toml" },
  { file = "src/ascend_ops/ascend-ops/**/*.rs" },
  { file = "src/ascend_ops/ascend-ops-cli/**/*.rs" },
  { file = "src/ascend_ops/ascend-ops-py/**/*.rs" },
]
